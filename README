To use:

Createa virtual environment for the tool you wish to use, and then access the script from there.

For instance:

$ virtualenv -p `which python3` env
$ bash
$ source env/bin/activate
$ pip install deuceclient
$ pip install deucevalere python-swiftclient
$ python deuceclient/deuceclient_file_tester.py
